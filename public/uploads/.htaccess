# Interdire l'acces direct aux fichiers uploades
# Les fichiers seront servis via un script PHP avec controle d'acces

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
